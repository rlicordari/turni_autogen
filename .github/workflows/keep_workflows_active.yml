name: Keep Scheduled Workflows Active (public repo)

on:
  schedule:
    - cron: "0 3 * * 1"   # ogni lunedì 03:00 UTC
  workflow_dispatch:

jobs:
  keepalive:
    runs-on: ubuntu-latest
    steps:
      - uses: gautamkrishnar/keepalive-workflow@v2
